/*
const navChoices = document.querySelectorAll('#header-left ul li a');
const arrowIcon = document.querySelector('.fa-angle-down');
const subMenus = document.querySelectorAll('.sub-menu');

const subMenu2 = document.querySelectorAll('#list-nav li');



navChoices.forEach( (navChoice) => {
    navChoice.addEventListener('click', test);
});

function test(e){

    subMenu2.forEach(sub => {
        console.log(sub.children[0]);
    })
    

};


*/











   /* subMenus.forEach((subMenu) => {
        subMenu.classList.toggle('active');
    }) */

   /* if(this.children[0]){
        this.children[0].classList.toggle('rotate');
        subMenu.classList.toggle('active');
    } */
    